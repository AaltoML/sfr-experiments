# @package _global_
defaults:
  - override /reward_model: cartpole
  - override /transition_model: sfr

alg_name: "sfr-sample"

transition_model:
  prediction_type: "GP-mean"

agent:
  unc_prop_strategy: "sample"
