# @package _global_
defaults:
  - override /reward_model: cartpole
  - override /transition_model: sfr

alg_name: "mlp"

transition_model:
  prediction_type: "NN-only"

agent:
  unc_prop_strategy: "mean"
